# Deprecated V1 Live Pipeline

This folder contains the old V1 live extraction scripts that queried the Spotify Web API directly. They were archived because Spotify restricts unlisted developer apps from using public search/catalog endpoints, triggering 400/404 errors. The project has since transitioned to a static Kaggle dataset and SQLite database layer.
